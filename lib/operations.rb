 def unsafe?
    puts unsafe("returns true if speed is greater than 60'")
      if = unsafe?(79)
      unsafe==true
    end

def not_safe?(speed)
	puts unsafe("'returns true if speed is less than 40'")
      if = unsafe?(35)
      unsafe==true
    end

def not_safe(speed)
    puts unsafe("'returns false if the speed is between 40 and 60'")
      if = unsafe?(50)
      unsafe==false
    end
  end


